<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:navigationBarColor">@color/yellowBurn</item>
        <item name="android:statusBarColor">@color/yellowBurn</item>
        <item name="android:colorBackground">@color/yellowLight</item>
    </style>

</resources>
